name: link-r-python-dev
channels:
  - conda-forge
dependencies:
  # kept in step with `environment.yml` - see the reasoning there
  - python
  - pandas>=2.3.3,<3
  - geopandas
  - movingpandas
  # dev dependencies
  - cartopy
  - geopy
  - geoviews
  - hvplot
  - matplotlib
  - jupyterlab
  # ./end dev dependencies